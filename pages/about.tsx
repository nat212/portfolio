import { NextPage } from 'next';
import Layout from '@components/layout';

const About: NextPage = () => {
    return (
        <Layout activePage='/about' pageTitle='About'>
            <div></div>
        </Layout>
    );
};

export default About;
